const sum = function(a, b){
    return a + b;
};

const subtract = (a , b) =>{
    return a - b;
}

const multiply = (a,b) => a*b;

const divide = null;

const modulus = (a,b) => {
    return a%b;
    // 10%5 - 0
    // 12%5 - 2
};

const even = null;

const odd = null;

module.exports = { 
    sum, 
    subtract, 
    multiply, 
    divide, 
    modulus, 
    even, 
    odd 
};
